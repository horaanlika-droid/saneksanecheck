services:
  portfolio:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      PORT: 8000
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
